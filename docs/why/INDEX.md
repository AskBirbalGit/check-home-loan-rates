# Why index

An agent-maintained map of the reasoning in this repo. Update it whenever you add a journal entry
or decision record. It's a convenience layer — `rg` over `docs/why/` (the `Tags`/`Touches`
fields) is always the source of truth. See `SEARCH.md`.

> Keep entries terse. Link to the journal day or decision file that holds the full why.

## Topics

| Tag | Where | Summary |
|-----|-------|---------|
| meta | [journal](journal/), [0001](decisions/0001-record-architecture-decisions.md) | why-journal setup and conventions; project-scoped opencode skills under `.opencode/skills/` (e.g. copywriting) |
| calculator | [journal](journal/), [0002](decisions/0002-single-file-static-calculator.md), [0005](decisions/0005-migrate-to-nextjs-for-vercel.md) | home loan rate + savings calculator (now a Next.js app — app/Calculator.tsx); blank-start bank combobox and CIBIL input, no empty logo chip, and Indian-grouped outstanding amount input; **bank name is optional** — with no bank, Box 2 drops the current-bank panel and shows a full-width "Best rates for your profile" list (CIBIL + employment only), reusing the same rate engine |
| home-loan | [journal](journal/) | CIBIL/employment lookup; spread-allocation rate curve (see rates) |
| rates | [journal](journal/), [0006](decisions/0006-mirror-based-rate-inheritance.md), [0008](decisions/0008-tier-aware-similar-lender-suggestions.md), [0009](decisions/0009-spread-allocation-cibil-rate-curve.md), [0010](decisions/0010-range-matched-mirror-targets.md), [0011](decisions/0011-cross-type-rate-matched-mirrors.md) | CIBIL-wise ROI data (Jun 2026); rate now from a **spread-allocation curve** (decision 0009) in lib/rate-engine.ts: per lender+employment, R_min=`800+` low, R_max=`<700` high, and score→cumulative fraction of that spread via fixed knots (800:0.03, 775:0.10, 750:0.20, 700:0.40, 600:1.00), linear within each band, raw rate **floored to lower 0.05%** (slices 3/7/10/20/60); mid-band published values + the old 600-649 uplift retired; expanded with long-tail lenders mirroring a first-party peer via `resolveBands` (now 78 institutions after 8 uncapturable high-yield + 14 editorially-removed lenders were dropped); mirror targets are **rate-matched across all types** (decision 0011, supersedes 0010's same-type rule) — each mirrored institution researched for its real public low/high home-loan range (official sources first, Paisabazaar/BankBazaar last resort) and mapped to whichever first-party lender (ANY type, crisscross allowed) minimises `|Δlow|+|Δhigh|` against that lender's `[R_min,R_max]` band, ties to closer floor (deterministic, see docs/rate-mirror-mapping.md); display type label is unchanged (only the curve is borrowed); 8 lenders whose band exceeds the ~16% first-party cap (Aptus, SRG, Star, Five-Star, Roha, Shubham, Mahindra Rural, Indostar) removed rather than misrepresented; Union Bank of India copies Canara Bank's exact bands; similar-institution suggestions are tier-aware (PSU>PVT>SFB>HFC): 2 same-tier + 1 stretch pick — stretch can span tiers and takes the cheapest (HFC→SFB/PVT, SFB→PVT/HFC, PVT→PSB, PSB→3 same-tier); mirrored lenders excluded, displayed rate-ascending |
| leads | [journal](journal/), [0007](decisions/0007-browser-side-lead-logging-with-supabase.md) | browser-side funnel logging for rate-check and savings steps into Supabase `loan_leads`; per-session UUID, INSERT-then-UPDATE (no upsert — table is write-only for anon); verified end-to-end against the live DB |
| supabase | [journal](journal/), [0007](decisions/0007-browser-side-lead-logging-with-supabase.md) | browser Supabase client (lib/supabase.ts, public NEXT_PUBLIC_ keys) + RLS-backed `loan_leads` table (supabase/schema.sql) for anonymous funnel logging; anon INSERT/UPDATE `to public` + table grant, no SELECT |
| savings | [journal](journal/) | EMI/tenure savings math; "total saved" hero + two-ways boxes (reduce-EMI ₹/mo, reduce-tenure time) in lib/savings.ts; reduce-tenure total now amortises the partial final month (was overstating EMIs paid → understating savings by ~½ EMI); the "already beats fair rate" branch (`nr >= cur`) renders as a green win (`savingsErrTone` + `.err--ok`), copy "You're already winning… Keep paying it down" — not the red error style |
| ui | [journal](journal/) | stacked-box layout (now app/Calculator.tsx, a React client component), one full-width rectangle per step, each with a section `<h2>` heading above its card (Your details / Rates for your profile / Additional loan details for savings / Savings on rate reduction; cols "In current institutions" / "Similar institutions"); Birbal design-system restyle; section cards carry a light-teal wash (`.box .card`) mirroring the hero panel; the Box 4 savings panels (`.sv-hero`/`.sv-card-*`) share that same frame — accent-24% border + shadow-sm, with the hero alone at shadow-md as the one allowed accent; the two `.sv-card`s are now title + former-eyebrow sub + two metric tiles (badge pill + descriptive sub-line + EMI/Tenure facts footer removed); Box 2 foot-note carries a `*` (`.rate-note`) that reveals a 3-bullet tooltip (`.rate-note-pop`) on hover/focus explaining how rates are derived — focusable span so it toggles on tap on phones |
| typography | [journal](journal/) | consolidated phone-first type ramp in css/styles.css :root: **7 sizes** (display/heading/money/lead/body 16/label 13/eyebrow 12 — the big 4 use `clamp()` so they step down on phone), **4 weights** (400/500/600/700 as an emphasis axis), **3 line-heights** (1.1/1.4/1.55), **3 letter-spacing tokens** (--ls-display/-tight/-eyebrow); EB Garamond → display(hero+showcase ₹)+headings (savings hero amount `.sv-hero-amt` now Figtree), Figtree → all body/UI + tabular data figures; fonts loaded via `next/font` (app/layout.tsx), self-hosted at build |
| architecture | [journal](journal/), [0003](decisions/0003-modular-files-for-parallel-agents.md), [0005](decisions/0005-migrate-to-nextjs-for-vercel.md) | Next.js (App Router) + React + TS; rate/savings logic in typed lib/*.ts modules |
| branding | [journal](journal/) | teal circle favicon at `app/icon.svg` (Next.js App Router auto-favicon), filled with the literal CTA teal `#46b8c3` to match `button.cta`; CTA recolour; hero now carries the Birbal brand logo (`public/brand-logo.jpg`) on the left, title/subtitle right-aligned; link-share thumbnail (`public/og-image.png`, brand logo on a solid cream 1200×630 canvas) wired via Open Graph metadata |
| social, seo | [journal](journal/) | link-preview / share-thumbnail metadata in app/layout.tsx: `metadataBase` (from `VERCEL_PROJECT_PRODUCTION_URL`, localhost fallback) + Open Graph + Twitter `summary_large_image`, image is `public/og-image.png` (1200×630) generated by `scripts/make-og-image.mjs` (sharp composites brand-logo onto a solid cream canvas; WhatsApp needs an absolute URL + opaque 1.91:1 raster) |
| logos | [journal](journal/) | lender logos fetched from logo.dev into `public/logos/`; rendered on rate rows + picker via slug match in app/Calculator.tsx; added logo assets for the expanded lender set |
| banks | [journal](journal/), [0004](decisions/0004-hardcoded-disbursement-ranking.md) | bank picker: searchable combobox, disbursement-ranked open order, type-to-filter, alias matching (e.g. "state bank of india"→SBI) in lib/banks.ts + app/Calculator.tsx; DISBURSE_RANK + ALIASES extended for the expanded lender set, then trimmed to 78 institutions (14 removed editorially) |
| deploy | [journal](journal/), [0005](decisions/0005-migrate-to-nextjs-for-vercel.md) | Vercel-native Next.js deploy (auto-detected, no vercel.json); security + cache headers in next.config.ts; static prerender + client hydration |
| dataset | [journal](journal/) | UI-scraped rate datasets (values from the rendered UI, not the rate engine). (1) `scripts/generate-ui-rate-dataset.mjs` — 32 first-party institutions × CIBIL points × Salaried/SE, current rate + the 3 better-priced lenders the UI surfaces (baseline `data/ui-rate-dataset.csv` 320 rows; timestamped re-runs add a 790 point / 10 fixed points). (2) `scripts/generate-all-institution-rates.mjs` — *all* current institutions (78 after removals: 32 first-party + 46 mirrored), 10 fixed CIBIL points × Salaried/SE, one row per (institution, type, CIBIL, employment) → interest rate; latest `data/all-institution-rates-20260625-174222.csv` (1560 rows). Both boot `next dev`, drive Box 1 with `agent-browser`, scrape Box 2; `OUTPUT_CSV` overrides the timestamped output |

## Entities

| Entity | Where touched | Why |
|--------|---------------|-----|
| docs/why | [journal](journal/) | Established why-tracking for the repo |
| index.html | [journal](journal/) | Removed — original side-by-side v1 page, superseded by index-v2.html |
| data/cibil-roi-bands | [journal](journal/) | Embedded rate bands from the ROI sheet |
| RateEngine.rateFor | [journal](journal/), [0006](decisions/0006-mirror-based-rate-inheritance.md), [0009](decisions/0009-spread-allocation-cibil-rate-curve.md), [0010](decisions/0010-range-matched-mirror-targets.md), [0011](decisions/0011-cross-type-rate-matched-mirrors.md) | Spread-allocation curve: R_min=`800+` low, R_max=`<700` high, score→cumulative spread fraction via fixed knots, linear within each band, raw rate rounded to nearest 0.05%; follows mirrored lenders to their rate-matched peer's bands (mirror targets chosen by closest researched low/high across any type, decision 0011) |
| calc/emi | [journal](journal/) | EMI amortisation function |
| calc/savings | [journal](journal/) | Two-option savings calculation |
| css/styles.css | [journal](journal/) | Shared tokens + component styles (v2 reuses them); global type tokens (font families + fw/fs/lh) live in its `:root`; `--font-head`/`--font-body` now point at `next/font` CSS vars; active segmented-control pill is a raised white/navy-text pill (not navy fill) so it doesn't clash with the navy CTA |
| font/eb-garamond, font/figtree | [journal](journal/) | Brand fonts loaded via `next/font` (app/layout.tsx), self-hosted at build; EB Garamond → title + headings, Figtree → all body/UI |
| js/savings.js | [journal](journal/) | Removed with v1 — was the mounted savings sub-calculator UI for index.html |
| js/data.js | [journal](journal/) | Removed in the Next.js migration — `RateEngine` logic ported verbatim to lib/rate-engine.ts |
| js/app.js | [journal](journal/) | Removed with v1 — was the page glue for index.html |
| data/cibil-roi-rates.csv | [journal](journal/) | Source ROI sheet copied into the repo; Union Bank of India row now matches Canara Bank's rate bands; Bajaj Housing Finance `<700` SE band is `9.00–11.00` |
| cibil/600-649 | [journal](journal/) | Former lowest CIBIL band; the derived `<700`+uplift computation was **retired by decision 0009** — the 600-700 slice (40% of each lender's spread) now carries the bottom of the spread-allocation curve directly |
| lender/Bajaj Housing Finance | [journal](journal/) | `<700` self-employed HFC rate capped at `9.00–11.00` instead of `9.00–20.00+` |
| docs/rate-mirror-mapping.md | [journal](journal/), [0010](decisions/0010-range-matched-mirror-targets.md), [0011](decisions/0011-cross-type-rate-matched-mirrors.md) | Rate-matched mapping table for the mirrored institutions: each one's researched public low/high home-loan range (official sources first, Paisabazaar/BankBazaar last resort), chosen first-party mirror (closest `[R_min,R_max]` band across ANY type, decision 0011 — same-type fence dropped), the match distance, and per-row reasoning; 8 uncapturable high-yield lenders marked "— removed" |
| docs/why/decisions/0007-browser-side-lead-logging-with-supabase.md | [journal](journal/) | Browser-side lead logging to Supabase using a write-only `loan_leads` table |
| lib/supabase.ts | [journal](journal/), [0007](decisions/0007-browser-side-lead-logging-with-supabase.md) | Cached browser Supabase client from public NEXT_PUBLIC_ keys; returns null (logging disabled) when env is missing |
| lib/leads.ts | [journal](journal/), [0007](decisions/0007-browser-side-lead-logging-with-supabase.md) | Funnel logging: per-session UUID, logRateCheck (INSERT-then-UPDATE), logSavings (UPDATE); fire-and-forget so logging can't break the calculator; `RateCheckLog.bankName` is nullable (bank is optional) |
| supabase/schema.sql | [journal](journal/), [0007](decisions/0007-browser-side-lead-logging-with-supabase.md) | loan_leads table + updated_at trigger + RLS (anon INSERT/UPDATE `to public`, table grant, no SELECT); run in the Supabase SQL editor |
| env/NEXT_PUBLIC_SUPABASE_URL, env/NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY | [journal](journal/), [0007](decisions/0007-browser-side-lead-logging-with-supabase.md) | Public browser-safe Supabase config in .env.local (gitignored) + documented in .env.example; set the same in Vercel env |
| scripts/fetch-logos.sh | [journal](journal/) | Reproducible logo.dev downloader (name→domain→slug, 128px PNG) |
| public/logos/ | [journal](journal/) | lender logos pulled from logo.dev in Next's static root; expanded with new lender logos |
| vercel.json | [journal](journal/) | Removed in the Next.js migration — Vercel auto-detects the framework; headers moved to next.config.ts |
| .vercelignore | [journal](journal/) | Removed in the Next.js migration |
| app/Calculator.tsx | [journal](journal/) | The whole stacked-box flow as a React client component (ported from index-v2.html + js/app-v2.js); owns the bank combobox state + journey-aware footer; hero h1 title-cased "Are You Paying the Right Home Loan Rate?"; bank combobox and CIBIL score now start blank, the logo chip only shows after selection, CIBIL input accepts 600-850, and the outstanding amount field formats Indian separators; on-page copy (labels/CTAs/headings/errors/footer) rewritten via the copywriting + humanizer skills, hero text left unchanged; Box 2 foot-note has a focusable `*` tooltip explaining how rates are derived (sub-700 private-bank reluctance, NBFC/HFC odds on small tickets, own market research) |
| app/layout.tsx, app/page.tsx | [journal](journal/) | Next App Router root: layout loads next/font + the two stylesheets; page renders Calculator; `<title>` kept in sync with the hero h1; layout `metadata` also carries Open Graph + Twitter share tags (`metadataBase` from `VERCEL_PROJECT_PRODUCTION_URL`, `/og-image.png` 1200×630) so shared links show the Birbal logo as the thumbnail |
| scripts/make-og-image.mjs | [journal](journal/) | Build-time sharp script that composites `public/brand-logo.png` onto a solid `#faf8f4` 1200×630 canvas → `public/og-image.png`; opaque + 1.91:1 so WhatsApp/iMessage/Slack render the share thumbnail cleanly |
| public/og-image.png | [journal](journal/) | Generated 1200×630 social-share thumbnail (Birbal logo on cream); referenced by Open Graph/Twitter metadata in app/layout.tsx |
| lib/rate-engine.ts | [journal](journal/), [0006](decisions/0006-mirror-based-rate-inheritance.md), [0008](decisions/0008-tier-aware-similar-lender-suggestions.md), [0009](decisions/0009-spread-allocation-cibil-rate-curve.md) | Typed RateEngine: **spread-allocation curve** (decision 0009) — per lender+employment R_min=`800+` low / R_max=`<700` high, `curveFraction(score)` cumulative-fraction knots (linear within each band), raw rate rounded to nearest 0.05% via `roundToRateStep`; old per-band averaging + 600-649 uplift removed; plus `mirror`/`resolveBands` inheritance for long-tail lenders; mirror targets bucketed by observed public rate ranges (docs/rate-mirror-mapping.md); Union Bank of India uses Canara Bank's exact bands; Bajaj Housing Finance `<700` SE is `9.00–11.00`; `others()` is tier-aware (STRETCH_TIERS) — 2 same-tier + 1 cheapest stretch pick spanning tiers (HFC→SFB/PVT, SFB→PVT/HFC, PVT→PSB, PSB→3 same-tier), mirrored lenders excluded, pool fallback, final rate-ascending sort |
| lib/banks.ts | [journal](journal/) | DISBURSE_RANK + ALIASES + ALL_BANKS + logoSlug/initials for the picker; extended for the expanded lender set |
| lib/leads.ts | [journal](journal/), [0007](decisions/0007-browser-side-lead-logging-with-supabase.md) | Best-effort funnel logging helpers for Box 1/2 and Box 3/4 into Supabase `loan_leads` |
| lib/supabase.ts | [journal](journal/), [0007](decisions/0007-browser-side-lead-logging-with-supabase.md) | Browser Supabase client using public `NEXT_PUBLIC_` env vars; returns null when unset |
| lib/savings.ts | [journal](journal/) | EMI amortisation + two-strategy switching-savings math (verbatim port) |
| supabase/schema.sql | [journal](journal/), [0007](decisions/0007-browser-side-lead-logging-with-supabase.md) | `loan_leads` table, timestamp trigger, and anonymous INSERT/UPDATE RLS for funnel logging |
| .env.example | [journal](journal/), [0007](decisions/0007-browser-side-lead-logging-with-supabase.md) | Public Supabase env names for browser lead logging |
| next.config.ts | [journal](journal/) | Security + logo cache headers (ported from the old vercel.json) |
| css/styles-v2.css | [journal](journal/) | Stacked-layout overrides on top of styles.css tokens/components; `.box .card` light-teal section wash mirroring the hero panel; `.rate-note`/`.rate-note-pop` foot-note tooltip (hover/focus-reveal, edge-to-edge on phones) |
| .opencode/skills/copywriting | [journal](journal/) | Project-scoped conversion-copywriting skill (from coreyhaines31/marketingskills) for writing/improving on-page marketing copy; auto-discovered by opencode under `.opencode/skills/**/SKILL.md` |
| scripts/generate-ui-rate-dataset.mjs | [journal](journal/) | Node + `agent-browser` CLI automation that boots `next dev`, drives Box 1 inputs and scrapes Box 2 rates for 32 first-party institutions × CIBIL score points × Salaried/SE; output defaults to a timestamped `data/ui-rate-dataset-<UTC>.csv` (`OUTPUT_CSV` overrides); score points: 600 + 625/675/725/775/825 (±15 randomized) + 790 + 850 |
| data/ui-rate-dataset.csv | [journal](journal/) | Generated 320-row UI-scraped baseline: per institution/band/employment, the current rate plus the three better-priced lenders the UI surfaces; re-runs go to timestamped siblings (e.g. data/ui-rate-dataset-20260625-072358.csv, 512 rows incl. the added 790 point) |
| scripts/generate-all-institution-rates.mjs, data/all-institution-rates-*.csv | [journal](journal/) | All-institutions per-rate simulation: scrapes the Box 2 "Your current bank" rate for every current institution (78) × 10 fixed CIBIL points × Salaried/SE; columns institution/institution_type/cibil_score/employment_type/interest_rate; `LENDER_TYPES` list kept in exact set-match with the engine's `LENDERS`; latest output data/all-institution-rates-20260625-174222.csv (1560 rows) |
## Decisions

| # | Title | Status |
|---|-------|--------|
| [0001](decisions/0001-record-architecture-decisions.md) | Record architecture decisions | Accepted |
| [0002](decisions/0002-single-file-static-calculator.md) | Single-file static calculator with embedded rate data | Superseded by 0005 |
| [0003](decisions/0003-modular-files-for-parallel-agents.md) | Modular files so multiple agents can work in parallel | Accepted |
| [0004](decisions/0004-hardcoded-disbursement-ranking.md) | Hand-maintained disbursement ranking for the bank picker | Accepted |
| [0005](decisions/0005-migrate-to-nextjs-for-vercel.md) | Migrate to Next.js (App Router) for first-class Vercel deployment | Accepted (supersedes 0002) |
| [0006](decisions/0006-mirror-based-rate-inheritance.md) | Mirror-based rate inheritance for the long-tail lenders | Accepted (per-band averaging math superseded by 0009; mechanism stays, mirror *targets* re-chosen by 0010) |
| [0007](decisions/0007-browser-side-lead-logging-with-supabase.md) | Browser-side lead logging with Supabase | Accepted |
| [0008](decisions/0008-tier-aware-similar-lender-suggestions.md) | Tier-aware "better-priced lenders" suggestions | Accepted |
| [0009](decisions/0009-spread-allocation-cibil-rate-curve.md) | Spread-allocation CIBIL rate curve | Accepted (supersedes 0006's per-band averaging) |
| [0010](decisions/0010-range-matched-mirror-targets.md) | Range-matched mirror targets for long-tail lenders | Superseded by 0011 |
| [0011](decisions/0011-cross-type-rate-matched-mirrors.md) | Cross-type rate-matched mirror targets | Accepted (drops 0010's same-type fence) |
