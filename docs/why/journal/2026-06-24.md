# Journal — 2026-06-24

> One entry per task. Newest at the bottom. Capture the *why*, not just the *what*.
> `Tags` and `Touches` make entries greppable — keep them concrete and consistent.

## Keep only expanded institutions

- **Ask:** Discard the current working-tree changes except the added institutions.
- **Changed:** Kept the expanded lender list in `lib/rate-engine.ts`, disbursement ranks and aliases in `lib/banks.ts`, new logo domain rows in `scripts/fetch-logos.sh`, new logo assets under `public/logos/`, and decision record `0006`. Restored the unrelated calculator UI/CSS/property/pension/current-rate changes.
- **Why:** The institution expansion is data/support work: new lenders need to appear in the picker, sort and match correctly, resolve rates through the closest existing peer where no first-party rate sheet exists, and have matching logos. The pension/property/rate-teaser UI changes were separate product behavior and were intentionally discarded.
- **Tags:** rates, banks, logos
- **Touches:** lib/rate-engine.ts, lib/banks.ts, scripts/fetch-logos.sh, public/logos/, RateEngine.rateFor, docs/why/decisions/0006-mirror-based-rate-inheritance.md
- **Decision:** [0006](../decisions/0006-mirror-based-rate-inheritance.md)
- **Agent:** OpenCode (gpt-5.5)

## Type-aware similar institution tie-breaks

- **Ask:** When similar institution rates tie, sort PSU/PSB lenders first, private lenders second, and other institution types after that; keep mirror-source names out of suggestions.
- **Changed:** Updated `lib/rate-engine.ts` so `others()` keeps rate as the primary sort, then applies institution type rank (`PSB`, `PVT`, others), then alphabetical name order.
- **Why:** Equal-rate suggestions should feel institutionally grouped rather than arbitrary. Mirror inheritance remains a data-layer detail: suggestions continue to display each lender's own name, not the peer whose rates it inherits.
- **Tags:** rates, banks, ui
- **Touches:** lib/rate-engine.ts, RateEngine.others, RateEngine.rateFor
- **Decision:** —
- **Agent:** OpenCode (gpt-5.5)
