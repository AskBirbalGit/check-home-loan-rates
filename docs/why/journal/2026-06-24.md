# Journal — 2026-06-24

> One entry per task. Newest at the bottom. Capture the *why*, not just the *what*.
> `Tags` and `Touches` make entries greppable — keep them concrete and consistent.

## Keep only expanded institutions

- **Ask:** Discard the current working-tree changes except the added institutions.
- **Changed:** Kept the expanded lender list in `lib/rate-engine.ts`, disbursement ranks and aliases in `lib/banks.ts`, new logo domain rows in `scripts/fetch-logos.sh`, new logo assets under `public/logos/`, and decision record `0006`. Restored the unrelated calculator UI/CSS/property/pension/current-rate changes.
- **Why:** The institution expansion is data/support work: new lenders need to appear in the picker, sort and match correctly, resolve rates through the closest existing peer where no first-party rate sheet exists, and have matching logos. The pension/property/rate-teaser UI changes were separate product behavior and were intentionally discarded.
- **Tags:** rates, banks, logos
- **Touches:** lib/rate-engine.ts, lib/banks.ts, scripts/fetch-logos.sh, public/logos/, RateEngine.rateFor, docs/why/decisions/0006-mirror-based-rate-inheritance.md
- **Decision:** [0006](../decisions/0006-mirror-based-rate-inheritance.md)
- **Agent:** OpenCode (gpt-5.5)

## Type-aware similar institution tie-breaks

- **Ask:** When similar institution rates tie, sort PSU/PSB lenders first, private lenders second, and other institution types after that; keep mirror-source names out of suggestions.
- **Changed:** Updated `lib/rate-engine.ts` so `others()` keeps rate as the primary sort, then applies institution type rank (`PSB`, `PVT`, others), then alphabetical name order.
- **Why:** Equal-rate suggestions should feel institutionally grouped rather than arbitrary. Mirror inheritance remains a data-layer detail: suggestions continue to display each lender's own name, not the peer whose rates it inherits.
- **Tags:** rates, banks, ui
- **Touches:** lib/rate-engine.ts, RateEngine.others, RateEngine.rateFor
- **Decision:** —
- **Agent:** OpenCode (gpt-5.5)

## Hide mirrored lenders from similar suggestions

- **Ask:** Institutions whose rates are inherited via `mirror` should not appear in the "Similar institutions" results.
- **Changed:** Updated `lib/rate-engine.ts` so `others()` filters out mirrored lenders from the suggestion candidate pool before rate sorting.
- **Why:** Mirrored lenders are included so users can find their current institution, but they do not carry first-party rate sheets. The comparison list should show only lenders with their own rate bands, while a mirrored current institution can still resolve its current-profile rate through its mapped peer.
- **Tags:** rates, banks, ui
- **Touches:** lib/rate-engine.ts, RateEngine.others, RateEngine.rateFor
- **Decision:** —
- **Agent:** OpenCode (gpt-5.5)

## Match best-profile rate strip to page background

- **Ask:** Make the "New rate from your best profile match" background match the whole page.
- **Changed:** Updated `css/styles.css` so `.save-newrate` uses the page `--bg` fill and the standard warm border instead of the older blue-tinted pill.
- **Why:** The line is contextual helper text inside the savings-input step, not a primary callout. Matching the page background keeps it visible without introducing a competing blue block inside the light-teal section card.
- **Tags:** ui, calculator
- **Touches:** css/styles.css, ui/savings-input, ui/section-backgrounds
- **Decision:** —
- **Agent:** OpenCode (gpt-5.5)

## Remove best-profile rate strip from savings inputs

- **Ask:** Remove the "New rate from your best profile match" line altogether.
- **Changed:** Deleted the `.save-newrate` block from `app/Calculator.tsx` and removed its unused styles from `css/styles.css`.
- **Why:** The recommended rate is already reflected in the savings calculation flow. Removing the extra context strip simplifies the savings input card and avoids repeating a figure that is not needed at that step.
- **Tags:** ui, calculator
- **Touches:** app/Calculator.tsx, css/styles.css, ui/savings-input
- **Decision:** —
- **Agent:** OpenCode (gpt-5.5)

## Re-bucket mirrored institutions by public rate range

- **Ask:** Some mirrored rates looked wrong. Research tentative public rates for every new/mirrored institution (2-3 references each), present a mapping table with reasoning for approval, then remap each institution to the closest first-party peer by observed rate band. Use Scrapling for the research pass.
- **Changed:** Updated 24 `mirror` targets in `lib/rate-engine.ts` (PSB/PVT/SFB/HFC). Added `docs/rate-mirror-mapping.md` with the approved table: institution, indicative rate range, sources, mapped lender, and per-row reasoning. Installed the Scrapling parser (`pip install --no-deps scrapling` + cssselect/w3lib/orjson/tldextract) for the research pass.
- **Why:** The earlier mapping leaned on broad institution type, so affordable/high-yield HFCs (Aptus, Shriram, Motilal Oswal, Altum Credo, Roha, Mahindra Rural) and high-spread NBFCs (SMFG, Piramal, SRG, Svatantra) inherited prime curves that understated their real rates. Each mapping is now anchored on the first-party lender's own starting rate (e.g. 800+ salaried) so the mirrored institution lands in a comparable band: Bandhan -> RBL (8.41 start, not IDFC 7.75), Federal/South Indian/City Union -> Axis, J&K/Nainital -> Bank of Baroda (PSU-parent pricing), Fincare -> AU (post-merger), Easy Home Finance -> Aavas (~9 start). Scrapling's full browser/fetcher install failed on this machine (its pyobjc dep won't build under system Python 3.9) and static Google HTML returns an enable-JS gate, so organic search-result scraping couldn't rank links; sources are the lender/aggregator pages reviewed per institution.
- **Tags:** rates, banks, home-loan
- **Touches:** lib/rate-engine.ts, docs/rate-mirror-mapping.md, RateEngine.rateFor, deps/scrapling
- **Decision:** [0006](../decisions/0006-mirror-based-rate-inheritance.md)
- **Agent:** OpenCode (claude-opus-4-8-thinking)

## Swap order of the two savings cards

- **Ask:** Swap the positioning of the two savings strategy cards — put "Reduce Tenure Keeping EMI Same" (Debt-Free Faster) on the left and "Reduce EMI Keeping Tenure Same" (More Cash Monthly) on the right, keeping everything else the same.
- **Changed:** Reordered the two `.sv-card` blocks inside `.sv-cards` in `app/Calculator.tsx` so the dark "Debt-Free Faster" card renders first (left) and the light "More Cash Monthly" card renders second (right). No content, copy, styling, or class changes.
- **Why:** Product wanted the debt-free/tenure-reduction story to lead the pair. Since the cards are laid out by source order in the CSS grid (no `nth-child`/position-based styling), swapping the JSX blocks is enough — each card keeps its own `sv-card-light`/`sv-card-dark` palette and metrics.
- **Tags:** ui, savings
- **Touches:** app/Calculator.tsx, .sv-cards, .sv-card-light, .sv-card-dark
- **Decision:** —

## Install copywriting skill

- **Ask:** Install the `copywriting` skill from skills.sh (coreyhaines31/marketingskills) into this repo.
- **Changed:** Added `.opencode/skills/copywriting/SKILL.md` plus its `references/copy-frameworks.md` and `references/natural-transitions.md`, sourced from the upstream repo. Installed under `.opencode/skills/` (project scope) rather than `~/.claude/skills/` so it's versioned with the repo and available to anyone working in it.
- **Why:** This is a conversion-copywriting marketing app (homepage/landing/savings UI); a copywriting skill is directly useful for writing/improving the on-page copy. Chose project-scoped `.opencode/skills/` over global so the skill travels with the repo and is shared, matching how opencode auto-discovers `**/SKILL.md` under `.opencode/skills`. Copied the files directly (the `npx skills add` CLI installs to Claude/global locations) to keep it self-contained and committable.
- **Tags:** meta, ui
- **Touches:** .opencode/skills/copywriting/SKILL.md, .opencode/skills/copywriting/references/copy-frameworks.md, .opencode/skills/copywriting/references/natural-transitions.md
- **Decision:** —
- **Agent:** OpenCode (claude-opus-4-8-thinking)

## Add browser-side lead logging

- **Ask:** Log the rate-check and savings funnel steps to Supabase without adding a server API.
- **Changed:** Added `lib/supabase.ts` for the browser client, `lib/leads.ts` for the funnel upsert/update path, `supabase/schema.sql` for the `loan_leads` table + RLS, `.env.example` for the public env vars, and wired `app/Calculator.tsx` to call the logging helpers. Updated `package.json`/`package-lock.json` for `@supabase/supabase-js` and `.gitignore` for local env files.
- **Why:** The app stays static while still capturing structured funnel data in one row per session. Using the publishable key with RLS keeps the table write-only from the browser and lets the calculator degrade silently if logging is unavailable.
- **Tags:** leads, supabase, calculator
- **Touches:** app/Calculator.tsx, lib/leads.ts, lib/supabase.ts, supabase/schema.sql, NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY, deps/@supabase/supabase-js
- **Decision:** [0007](../decisions/0007-browser-side-lead-logging-with-supabase.md)
- **Agent:** OpenCode (gpt-5.4-mini)

## Add Indian grouping to outstanding amount

- **Ask:** Remove the prefilled value from the first input box and show the outstanding amount with Indian number grouping.
- **Changed:** Updated `app/Calculator.tsx` so the bank combobox starts blank, the rate check prompts for a bank if none is selected, and the outstanding amount field formats digits as `##,##,###` style rupee input while still submitting a plain numeric value.
- **Why:** The first field should start from an empty state instead of assuming a bank, and the loan amount is easier to read when it follows the Indian separators users expect for home-loan figures.
- **Tags:** ui, calculator, savings
- **Touches:** app/Calculator.tsx, ui/bank-combobox, ui/outstanding-amount
- **Decision:** —
- **Agent:** OpenCode (gpt-5.4-mini)

## Hide empty bank logo placeholder

- **Ask:** When no institution is selected, remove the blank-looking logo placeholder from the bank field.
- **Changed:** Updated the bank combobox in `app/Calculator.tsx` so the selected-logo chip only renders after a bank is chosen.
- **Why:** An empty logo slot reads like a broken or half-loaded state. Keeping the chip out of the DOM until a real selection exists makes the blank state clean and avoids a misleading visual placeholder.
- **Tags:** ui, banks, calculator
- **Touches:** app/Calculator.tsx, ui/bank-combobox, ui/bank-logo
- **Decision:** —
- **Agent:** OpenCode (gpt-5.4-mini)

## Rewrite page copy with copywriting + humanizer skills

- **Ask:** Use the copywriting skill to suggest copy for the whole page, then update the copy applying the humanizer skill. Keep the hero title and subtext unchanged.
- **Changed:** Updated on-page copy in `app/Calculator.tsx` — Box 1 label "Bank name"→"Your current bank" and its placeholder, CTA "Check my rates"→"Show my fair rate"; Box 2 heading→"The fair rate for your profile", column heads "In current institutions"→"Your current bank" and "Similar institutions"→"Better-priced lenders", foot-note rewrite, CTA "Check my savings"→"Show my savings"; Box 3 heading→"A few loan details to calculate your savings" and all four error strings; Box 4 heading→"Here's what switching saves you", card badges (added comma), and both card sub-lines; the full-footer constant `FOOTER_FULL`. Hero `<h1>`/`<p class="sub">` left as-is per the ask, so `layout.tsx`/`page.tsx` titles stay in sync untouched.
- **Why:** Copy is benefit-led and specific (skill: benefits over features, strong action+outcome CTAs, customer language like "your current bank"/"better-priced lenders"). Then ran it through the humanizer skill: removed the em dash/comma-spliced "leaving money on the table — see exactly how much" into two sentences, cut hedge/filler wording in the error strings ("Please fill...with valid values"→"Add your current rate, remaining tenure, and outstanding amount..."), and kept sentences plain with `is/has` rather than inflated phrasing. No computed values (rate-cut %, savings figures, EMI math) were touched — only static copy.
- **Tags:** ui, calculator, copy
- **Touches:** app/Calculator.tsx, FOOTER_FULL, ui/box1, ui/box2, ui/box3, ui/box4, .opencode/skills/copywriting, skills/humanizer
- **Decision:** —
- **Agent:** OpenCode (claude-opus-4-8-thinking)

## Shorten Box 3 heading and Box 2 foot-note

- **Ask:** The Box 3 heading and Box 2 foot-note read too long; shorten them.
- **Changed:** In `app/Calculator.tsx`, Box 3 heading "A few loan details to calculate your savings" → "Your loan details for savings.", and Box 2 foot-note "These are today's fair rates for someone with your profile. Paying more than this? You're leaving money on the table. See exactly how much." → "These are today's fair rates for your profile. Paying more? See what you could save."
- **Why:** Both lines were doing too much for a scan-and-go funnel step. The heading drops to four words; the foot-note keeps the rhetorical question and the save promise but cuts the "leaving money on the table" idiom that padded it without adding information.
- **Tags:** ui, calculator, copy
- **Touches:** app/Calculator.tsx, ui/box2, ui/box3
- **Decision:** —
- **Agent:** OpenCode (claude-opus-4-8-thinking)

## Floor suggested rates to 0.05%

- **Ask:** Keep using the averaged rate suggestions, but show the floor to the closest 0.05 percentage-point step, e.g. 7.79% -> 7.75%.
- **Changed:** Updated `lib/rate-engine.ts` so the granular averaged rate is floored to the nearest 0.05 before `rateFor()` returns it. Existing display formatting still renders two decimals.
- **Why:** The average remains the basis for CIBIL-position pricing, but the surfaced rate should land on conventional 5-bps lending increments and should never round above the computed average.
- **Tags:** rates, calculator, home-loan
- **Touches:** lib/rate-engine.ts, RateEngine.rateFor, RateEngine.others, RateEngine.bestRate
- **Decision:** —
- **Agent:** OpenCode (gpt-5.5)

## Match Union Bank rates to Canara Bank

- **Ask:** Change Union Bank of India rates to use exactly the Canara Bank rates.
- **Changed:** Updated the Union Bank of India row in `lib/rate-engine.ts` and the source `data/cibil-roi-rates.csv` to copy Canara Bank's eight CIBIL/employment rate-band cells.
- **Why:** The runtime lender remains a normal first-party row, so it still participates in suggestions and sorting as before, while every Union Bank lookup now resolves to the same bands as Canara Bank per the requested data correction.
- **Tags:** rates, banks, home-loan
- **Touches:** lib/rate-engine.ts, data/cibil-roi-rates.csv, lender/Union Bank of India, lender/Canara Bank, RateEngine.rateFor
- **Decision:** —
- **Agent:** OpenCode (gpt-5.5)

## Provision + verify Supabase lead logging end-to-end

- **Ask:** Actually wire the lead logging into the Supabase database with the provided creds, put the keys in env, and test it after implementing — verify rows land in the DB.
- **Changed:** Installed `@supabase/supabase-js`; added `.env.local` (gitignored) + `.env.example` with the public `NEXT_PUBLIC_SUPABASE_*` vars; `lib/supabase.ts` (cached browser client, null-safe), `lib/leads.ts` (per-session UUID, INSERT-then-UPDATE), `supabase/schema.sql` (loan_leads table + trigger + RLS), and wired `logRateCheck`/`logSavings` into `app/Calculator.tsx`. Tightened the RLS policies to `to public with check (true)` and added `grant insert, update ... to anon`.
- **Why:** Verifying against the live DB exposed two things the schema-only draft missed. (1) `upsert()` returned 401/42501 because PostgREST's ON CONFLICT path needs a SELECT policy to read the conflicting row back — but the table is write-only for anon by design. Switched `logRateCheck` from upsert to an `inserted`-flag INSERT-then-UPDATE so no SELECT is ever required. (2) The original `to anon` policy + missing table grant rejected inserts; `to public` + explicit grant fixes it. Confirmed via supabase-js (the exact app path): INSERT, re-check UPDATE, and savings UPDATE all return OK, and anon SELECT returns 0 rows (write-only holds). `npm run build` passes as a static export for Vercel.
- **Tags:** leads, supabase, calculator
- **Touches:** app/Calculator.tsx, lib/leads.ts, lib/supabase.ts, supabase/schema.sql, .env.local, .env.example, env/NEXT_PUBLIC_SUPABASE_URL, env/NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY, deps/@supabase/supabase-js
- **Decision:** [0007](../decisions/0007-browser-side-lead-logging-with-supabase.md)
- **Agent:** OpenCode (claude-opus-4-8-thinking)

## Remove default CIBIL score

- **Ask:** Remove CIBIL score default values too.
- **Changed:** Updated `app/Calculator.tsx` so the CIBIL score input starts blank instead of prefilled with `810`.
- **Why:** The bank picker already starts blank, and the CIBIL score should behave the same way so users provide their own profile data before rates are calculated. The existing validation still blocks rate checks until a valid 650-850 score is entered.
- **Tags:** ui, calculator, home-loan
- **Touches:** app/Calculator.tsx, ui/box1, input/cibil
- **Decision:** —
- **Agent:** OpenCode (gpt-5.5)

## Add 600-649 CIBIL band

- **Ask:** Add one more CIBIL range, 600-649, with derived rate uplifts of +0.50% for PSU/PSB, +0.50% for private banks, +0.75% for small finance banks, and +1.00% for HFCs.
- **Changed:** Updated `lib/rate-engine.ts` so scores 600-649 reuse the existing `<700` rate cells with lender-type uplifts before the existing 10-slice averaging and 0.05% floor. Updated `app/Calculator.tsx` to accept CIBIL scores from 600 and label the new range.
- **Why:** The source sheet has no native 600-649 columns, so deriving the band from the closest existing weak-CIBIL band preserves the current data model and mirror inheritance while applying the requested extra risk spread by institution type.
- **Tags:** rates, calculator, home-loan
- **Touches:** lib/rate-engine.ts, app/Calculator.tsx, RateEngine.rateFor, input/cibil, cibil/600-649
- **Decision:** —
- **Agent:** OpenCode (gpt-5.5)

## Correct 600-649 derivation and Bajaj weak-CIBIL SE rate

- **Ask:** Ensure the 600-649 CIBIL uplifts are added from the higher end of the existing weak-CIBIL rates, not the lower side, and reduce Bajaj Housing Finance's 650-699 rate from `9.00–20.00+` to `9.00–11.00`.
- **Changed:** Updated `lib/rate-engine.ts` so the derived 600-649 band uses the `<700` cell's high-rate endpoint plus the lender-type uplift. Updated Bajaj Housing Finance's `<700` self-employed HFC cell in `lib/rate-engine.ts` and `data/cibil-roi-rates.csv` to `9.00–11.00`.
- **Why:** The new lowest band should price off the weakest available part of the source band before applying the extra risk spread. Bajaj's earlier `20.00+` high end made the weak-CIBIL SE curve an outlier and distorted both direct lookups and mirrored HFC rates that inherit from Bajaj.
- **Tags:** rates, calculator, home-loan
- **Touches:** lib/rate-engine.ts, data/cibil-roi-rates.csv, RateEngine.rateFor, cibil/600-649, lender/Bajaj Housing Finance
- **Decision:** —
- **Agent:** OpenCode (gpt-5.5)

## Generate a UI-scraped rate dataset via agent-browser

- **Ask:** Add the `agent-browser` CLI (from the akarankaran/ai-toolkit repo) and use it to build a dataset, scraped from the UI rather than the backend. For all 32 first-party (non-mirrored) institutions, across 5 CIBIL bands (base 625/675/725/775/825, each randomized ±15) and both Salaried + Self-employed, capture the current institution's new rate plus the better-priced lender rates the UI shows.
- **Changed:** Added `scripts/generate-ui-rate-dataset.mjs` (Node script) and the generated `data/ui-rate-dataset.csv` (320 rows). The script boots `next dev` on port 3100, opens the page with `agent-browser`, and drives the real Box 1 controls (bank combobox, CIBIL input, employment segmented control, "Show my fair rate") then scrapes Box 2's `#currentRate`/`#otherRates` DOM for each scenario.
- **Why:** The point was to validate what the live UI actually outputs end-to-end, so values are read from the rendered page, never from `lib/rate-engine.ts`. Treated agent-browser as a **CLI tool** (per the user) rather than wiring it into opencode's MCP config — install was just the global npm binary that was already present (`agent-browser 0.29.1`), so no repo config changed. The first approach (one big async `eval` writing to `window.__uiDataset`, then polling) timed out: agent-browser's `eval` resolves the returned promise and prints its value, but the background poll never observed the `done` state reliably. Switched to one synchronous `eval --stdin` per scenario that returns the scraped row as a JSON string — slower but deterministic and easy to debug. CIBIL is randomized once per band and reused across all institutions/employment types so comparisons stay fair; the CSV records both the base band and the actual score used. The 32 institutions are exactly the first-party rows (those with their own `b` bands) — mirrored long-tail lenders are intentionally excluded since they don't carry first-party sheets and are already hidden from the "better-priced lenders" suggestions.
- **Tags:** rates, calculator, home-loan, ui
- **Touches:** scripts/generate-ui-rate-dataset.mjs, data/ui-rate-dataset.csv, app/Calculator.tsx, RateEngine.rateFor, deps/agent-browser
- **Decision:** —
- **Agent:** OpenCode (claude-opus-4-8-thinking)

## Tier-aware better-priced lender suggestions

- **Ask:** Suggest lenders along the realistic eligibility ladder (PSU > PVT > SFB > NBFC/HFC). For a customer of a given type, show two same-type suggestions plus one from the slightly better type (whichever is the next rung up); PSU customers get same-type only.
- **Changed:** Rewrote `others()` in `lib/rate-engine.ts` to be tier-aware. Added `TIER_ORDER`/`tierRank`/`betterTier` helpers and removed the old `suggestionTypeRank` global-cheapest sort. For tier T it returns 2 same-tier + 1 next-better-tier picks (3 same-tier for PSB, which has no better tier), each chosen cheapest-first, with a pool-wide cheapest-first fallback to always fill 3 rows, then a final rate-ascending sort for display. Mirrored lenders stay excluded from the pool. No UI change — `app/Calculator.tsx` keeps the "Better-priced lenders" heading and the unchanged `OtherRate` shape.
- **Why:** A flat "cheapest three anywhere" list oversold where a borrower can actually refinance — someone at an NBFC won't typically jump straight to a prime PSU, and is even less likely two rungs up. Two in-tier + one stretch option matches real refinancing behaviour. Per the planning Q&A: PSU capped at 3 rows (not "all PSUs"), same-tier picks are the cheapest in-tier regardless of whether they beat the current rate, mirrored lenders remain excluded (they'd duplicate their source's exact rate), and the final 3 are sorted purely by rate. Knock-on: `bestRate()` now mins over tier-constrained options, so savings reflect a realistic switch target rather than the absolute market floor — intentional, slightly lower savings for some profiles. Verified with a temporary tsx harness (3 rows, correct tier mix, no current bank/mirror, ascending rate across all four tiers × CIBIL 620/700/760/810 × sal/se) and `npm run build`; harness removed after.
- **Tags:** rates, banks, ui, home-loan
- **Touches:** lib/rate-engine.ts, RateEngine.others, RateEngine.bestRate
- **Decision:** [0008](../decisions/0008-tier-aware-similar-lender-suggestions.md)
- **Agent:** OpenCode (claude-opus-4-8-thinking)

## Multi-tier stretch slot for suggestions

- **Ask:** Refine the tier-aware stretch slot so HFC reaches SFB **or** PVT (whichever is lower) and SFB reaches PVT **or** HFC (whichever is lower); PVT→PSB and PSB→3 same-tier unchanged.
- **Changed:** Replaced the single `BETTER_TIER` map in `lib/rate-engine.ts` with `STRETCH_TIERS` (PSB:[], PVT:[PSB], SFB:[PVT,HFC], HFC:[SFB,PVT]). `others()` now builds stretch candidates across all of a tier's stretch tiers and takes the single cheapest, so the stretch row is "whichever is lower" rather than a fixed rung. Same-tier count, mirror exclusion, pool fallback, and final rate-ascending sort unchanged. Updated decision 0008 to match.
- **Why:** A fixed one-rung stretch overstated the ladder — an NBFC/HFC borrower's genuinely cheapest realistic alternative may be a private bank, not necessarily an SFB, and an SFB borrower's may be a PVT or another HFC. Picking the cheapest across both candidate tiers surfaces the real best option. Verified with a temporary tsx harness (3 rows, ≥2 same-tier, ≥1 stretch from the allowed tiers, no current/mirror, ascending rate across all four tiers × CIBIL 620/700/760/810 × sal/se) and `npm run build`; harness removed after.
- **Tags:** rates, banks, ui, home-loan
- **Touches:** lib/rate-engine.ts, RateEngine.others, RateEngine.bestRate
- **Decision:** [0008](../decisions/0008-tier-aware-similar-lender-suggestions.md)
- **Agent:** OpenCode (claude-opus-4-8-thinking)

## Enlarge logos within fixed chips

- **Ask:** Make the lender logos slightly bigger everywhere without changing the chip element sizes.
- **Changed:** Reduced the inner image `padding` on all three logo chips: `.bank-logo img` 4px→2px (`css/styles.css`), `.combo-logo img` 3px→1px and `.combo-selected-logo img` 2px→1px (`css/styles-v2.css`).
- **Why:** The chip box dimensions drive the surrounding layout, so shrinking the image padding grows the visible logo edge-to-edge while keeping the 36/26/24px chips — and everything aligned around them — unchanged.
- **Tags:** logos, ui
- **Touches:** css/styles.css, css/styles-v2.css, .bank-logo, .combo-logo, .combo-selected-logo
- **Decision:** —
- **Agent:** OpenCode (claude-opus-4-8-thinking)

## Simplify savings strategy cards + Figtree hero amount

- **Ask:** (1) Render the "₹35.83 L Saved." hero amount in Figtree at a similar size. (2) In both savings cards, drop the descriptive sub-line and the EMI/Tenure facts footer. (3) Turn each card's eyebrow pill into the subtext, keeping the existing titles.
- **Changed:** `css/styles-v2.css` `.sv-hero-amt` font-family `--brand-editorial`→`--brand-sans` (kept `--fs-display`). `app/Calculator.tsx`: removed both `.sv-badge` pills, replaced the two `.sv-sub` paragraphs with the former eyebrow copy ("Reduce Tenure, Keep EMI Same" / "Reduce EMI, Keep Tenure Same"), deleted both `.sv-facts` blocks (EMI/Tenure rows), and dropped the now-unused `fmtMonths`/`fmtYearsMonths` imports.
- **Why:** Product wanted the hero savings number in the sans (Figtree) face while holding the display size, so only the family swapped — the rest of the hero ramp is untouched. The cards were carrying redundant detail (a prose sub-line plus an EMI/Tenure footer that mostly repeated "(Same)"); collapsing each card to title + former-eyebrow sub + two metric tiles keeps the two strategies legible without the duplicated EMI/tenure data. Titles stayed ("Debt-Free Faster" / "More Cash Monthly") per the brief — only the eyebrow moved into the sub slot. The `.sv-badge`/`.sv-facts` CSS rules were left in place (dead but harmless, reusable). `next lint` + `tsc --noEmit` clean.
- **Tags:** ui, savings, typography
- **Touches:** css/styles-v2.css, app/Calculator.tsx, .sv-hero-amt, .sv-badge, .sv-sub, .sv-facts, font/figtree
- **Decision:** —
- **Agent:** OpenCode (claude-opus-4-8-thinking)

## Recolour primary CTA to teal with navy text

- **Ask:** Change the button colours to teal `#46b8c3`, with a white button label.
- **Changed:** `button.cta` in `css/styles.css`: `background` `var(--primary)` (navy) → `#46b8c3`, text `color` `var(--primary-fg)` (white) kept white `#ffffff`, and hover `background` `var(--primary-strong)` → `color-mix(#46b8c3 88%, black)`. Updated the section comment to match.
- **Why:** Product wanted the CTA to flip to the teal fill. Label colour churned through navy then black before landing on white per the follow-ups — white reads cleanest on the teal and matches the prior white-on-navy treatment. The teal was set as a literal `#46b8c3` since it doesn't map onto an existing token (the closest, `--accent-bright #25b4c4`, is a different hue); hover darkens that same literal via `color-mix` so it tracks the new base. Secondary/outline `.cta.secondary` left unchanged. `next lint` clean.
- **Tags:** ui, buttons, branding
- **Touches:** css/styles.css, button.cta, button.cta:hover
- **Decision:** —
- **Agent:** OpenCode (claude-opus-4-8-thinking)

## Bump savings hero amount to bold weight

- **Ask:** Increase the weight of the "₹3.54 Cr Saved." hero number — it's the highlight, and the thin weight wasn't working.
- **Changed:** `.sv-hero-amt` in `css/styles-v2.css`: `font-weight` `var(--fw-head)` (500) → `var(--fw-bold)` (700).
- **Why:** The amount had been carried over at the editorial 500 weight from when it was EB Garamond; after the earlier swap to Figtree (Cr/L amount as the page's hero stat) 500 read too light. `--fw-bold` (700) is the existing "emphasis: rate numbers, savings amounts" token and is already loaded for Figtree (app/layout.tsx weights 400/600/700), so it gives the highlight the heft it needs without a new weight. Size (`--fs-display`) unchanged.
- **Tags:** ui, savings, typography
- **Touches:** css/styles-v2.css, .sv-hero-amt, --fw-bold
- **Decision:** —
- **Agent:** OpenCode (claude-opus-4-8-thinking)
