# Journal — 2026-06-20

> One entry per task. Newest at the bottom. Capture the *why*, not just the *what*.
> `Tags` and `Touches` make entries greppable — keep them concrete and consistent.

## 19:30 — Set up why-journal

- **Ask:** Set up the repo using my ai-toolkit before building the home loan calculator.
- **Changed:** `docs/why/**`, `AGENTS.md`.
- **Why:** Establish why-tracking from day one so the reasoning behind every change is captured
  as work happens, not reconstructed later.
- **Tags:** meta
- **Touches:** docs/why, AGENTS.md
- **Decision:** [0001](../decisions/0001-record-architecture-decisions.md)
- **Agent:** OpenCode (claude-opus-4-8-thinking)

## 20:10 — Build home loan rate calculator

- **Ask:** A single-page calculator. User inputs bank, CIBIL, and salaried/self-employed, hits a
  check button, and sees their current institution's rate band plus 3 similar institutions for
  their profile. A "check savings" CTA then takes outstanding amount + current rate and shows
  overall savings under two options: keep EMI same and cut tenure, vs keep tenure same and cut EMI.
- **Changed:** `index.html` (new — full calculator: markup, styles, rate data, logic).
- **Why:** Built as a single self-contained `index.html` (no build step, no deps) so it opens
  in any browser and is trivial to host. Rate bands are embedded as a JS array transcribed from
  the provided CIBIL-wise ROI sheet (Jun 2026), keyed by CIBIL band (800+/750–799/700–749/<700)
  and salaried vs self-employed columns. "Other institutions" are picked as the 3 lowest
  low-bound rates for the same profile, excluding the current bank. Savings uses standard
  amortisation: Option A keeps the original EMI and solves for a shorter tenure (n via the loan
  closure formula); Option B keeps tenure and recomputes a lower EMI. New rate is pre-filled with
  the best fair rate shown on screen so the savings are grounded in the displayed bands.
- **Tags:** calculator, home-loan, rates, savings, ui
- **Touches:** index.html, data/cibil-roi-bands, calc/emi, calc/savings
- **Decision:** [0002](../decisions/0002-single-file-static-calculator.md)
- **Agent:** OpenCode (claude-opus-4-8-thinking)

## 21:05 — Granular averaged CIBIL rate lookup

- **Ask:** Make the CIBIL rate lookup 10x more granular and return a single averaged rate
  instead of a coarse band range.
- **Changed:** `js/data.js` (rateFor + helpers only; LENDERS data and public contract unchanged).
- **Why:** The four source bands now cover the full CIBIL 650–850 domain in 50-point bands
  (800→[800,850], 750–799→[750,800], 700–749→[700,750], <700→[650,700]). Within each band both
  the CIBIL coverage and the rate range are split into 10 equal slices; a higher score earns a
  lower rate, so the CIBIL slice index `i` (0 = bottom of band) maps to rate step `9 - i`, and the
  returned value is that step's midpoint `lo + (stepIndex + 0.5) * (hi - lo) / 10`, rounded to 3 dp.
  This reproduces the user's anchor: band 750–800 / rate 7.00–7.50, score 799 → 7.025. Single-value
  cells (Union "7.15") parse lo==hi so all slices return the same number; trailing-`+` cells have
  the `+` stripped (single-value-with-plus is flat; range-with-plus like "9.00–20.00+" uses its
  numeric lo/hi). Kept the `RateEngine` public contract (rateFor/formatRate/numericLow/others/
  bestRate/lenders/typeLabel) and signatures intact — `rateFor` now returns a number, which the
  existing helpers already support. Verified with a throwaway Node harness (16 assertions incl. the
  7.025 anchor, band-edge monotonicity, single-value & `+` parsing, bestRate==min, sorted others);
  `node --check` passes.
- **Tags:** rates, home-loan, calculator
- **Touches:** js/data.js, data/cibil-roi-bands, RateEngine.rateFor
- **Decision:** —
- **Agent:** OpenCode (claude-opus-4-8-thinking)

## 21:30 — Restructure savings sub-calculator (max-savings hero + two strategy boxes)

- **Ask:** On top of the rate results, give an extra calculator showing savings if the user's
  rate drops to the lowest rate from their suggested profile. After seeing their rate, ask for
  CURRENT RATE, TENURE, and REMAINING AMOUNT. Show savings as one BIG box for MAX savings, then
  two boxes below — one for reducing EMI, one for reducing tenure.
- **Changed:** `js/savings.js` (UI markup + result rendering restructured; math kept).
- **Why:** Reframed the result UX around the user's headline question ("what's the most I can
  save?"): a full-width hero box shows `maxSaving = max(reduceTenureTotal, reduceEmiTotal)` and
  names the winning strategy, then a 2-up grid breaks it down into the reduce-EMI and
  reduce-tenure boxes; the box matching the headline gets `.best`. Inputs were reordered to
  current rate → tenure → outstanding to match the asked flow, and an explicit read-only "New
  rate from your best profile match" line was added so the user understands what the savings are
  computed against. Critically, the new rate is sourced ONLY from `setContext.bestRate` (mirrored
  into a read-only input + the context line) and is never typed by the user, keeping savings
  grounded in the on-screen best rate. The public contract (`mount`/`setContext`/`reset`) and the
  existing `emi`/`inr0`/`fmtMonths` helpers (incl. Indian number grouping) are unchanged. Added a
  `.save-hero` class on the hero box, but it also carries `.save-card` so it stays readable before
  Agent 2 themes it. Verified with `node --check` and a throwaway pure-math test (P=40L, 8.60%→
  7.30%, 18y): emiOld≈36,465, reduce-tenure≈₹12.4L closing ~34 months sooner, reduce-EMI≈₹6.8L at
  ~₹3,140/mo lower, maxSaving == reduce-tenure.
- **Tags:** calculator, savings, ui
- **Touches:** js/savings.js, calc/savings, calc/emi
- **Decision:** —
- **Agent:** OpenCode (claude-opus-4-8-thinking)

## 22:15 — Restyle calculator to the Birbal design system

- **Ask:** Make the standalone calculator's CSS match the Birbal marketing-site design system
  (warm ivory + deep navy + turquoise, success-green savings, serif headings). Edit only
  `css/styles.css`; the HTML markup and JS-generated class names are a fixed contract.
- **Changed:** `css/styles.css` (full rewrite of tokens + rules).
- **Why:** Mirrored the reference's light "royal" theme tokens from
  `repo/website/src/app/globals.css` verbatim (`--bg #faf8f4`, `--surface #fff`, `--fg #0e2a40`,
  `--border #e9e1d5`, navy `--primary #134e7a`, turquoise `--accent #0e7c8c`/`--accent-bright
  #25b4c4`, `--success #0f8a5f`, `--gold #b7872c`, the `--shadow-*` set, `--radius-card 1.25rem`,
  `--ring`). Cards use the reference `.card` recipe (surface + warm hairline + `--shadow-sm` +
  card radius); the primary CTA is navy with a `--primary-strong` hover (matching button.tsx
  `primary`), the secondary CTA is the `outline` treatment; the segmented control became the
  inset navy-pill toggle from kit.tsx's `Segmented`. Result rows render as soft cards, the
  `.current` row gets a success/surface gradient tint, and `.out-note` uses the warm `--gold-soft`
  tint. Savings boxes use success-green; I also pre-styled the likely Agent-4 classes
  `.save-hero`/`.save-grid` (savings.js already emits `.save-hero`) so the new hero box themes
  correctly. Because this is a static page with no Next font pipeline, headings use a web-safe
  serif stack (`Georgia, "Times New Roman", serif`) standing in for EB Garamond and body uses
  `system-ui, -apple-system, "Segoe UI", Roboto, sans-serif` for Figtree — NO `@import`
  google-fonts, keeping it offline-safe. Kept the existing 860/540/480px breakpoints, a visible
  `--ring` focus state, and `.hidden { display:none }`. Verified every contract class has a rule
  and `npx csstree-validator` passes clean.
- **Tags:** ui, calculator
- **Touches:** css/styles.css, env/none, deps/none
- **Decision:** —
- **Agent:** OpenCode (claude-opus-4-8-thinking)

## 20:40 — Modularise calculator + run three changes in parallel

- **Ask:** Init git (Agent 1), then run three changes "in parallel": restyle to the Birbal design
  system (Agent 2), make CIBIL rates 10x granular over 650–850 (Agent 3), and rebuild the savings
  tool with a max-savings hero + reduce-EMI/reduce-tenure boxes (Agent 4).
- **Changed:** `index.html` (split into external refs), `css/styles.css`, `js/data.js`,
  `js/savings.js`, `js/app.js` (new), `data/cibil-roi-rates.csv` (new), `.gitignore`.
- **Why:** All three asks touched the single `index.html`, so true parallel sub-agents would
  collide on one file. I first split the monolith into one-owner-per-file modules with documented
  public contracts (`window.RateEngine` in data.js, `window.Savings` in savings.js, glue in
  app.js, presentation in styles.css). That let Agents 2/3/4 run concurrently with zero file
  conflicts. After they returned I integrated: restored a lender (JM Financial Services) that was
  dropped during data.js's rewrite (back to 32 lenders), added the `.save-newrate` CSS the savings
  UI emits, and ran a DOM-shim end-to-end test confirming the full flow (rate lookup → bestRate
  pre-fill → savings hero + two boxes) produces the known-good numbers (₹12.4L max / ₹6.8L EMI on
  the 40L · 8.60→7.30 · 18y case). `node --check` passes on all JS; csstree-validator passes.
- **Tags:** calculator, ui, architecture, meta
- **Touches:** index.html, css/styles.css, js/data.js, js/savings.js, js/app.js, data/cibil-roi-rates.csv
- **Decision:** [0003](../decisions/0003-modular-files-for-parallel-agents.md)
- **Agent:** OpenCode (claude-opus-4-8-thinking)
